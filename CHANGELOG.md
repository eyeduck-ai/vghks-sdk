# Changelog

## 0.20.0

- `records.get_soap` 新增 S／O／A+P、獨立 A／P、診斷碼與名稱，以及 SOAP 頁面中的醫囑／藥囑摘要。依明確標籤及跨列關係辨識，保留既有 `blocks`、`full_text` 與建構介面，不增加 HTTP 請求。
- 新增 `SoapDiagnosis`、`SoapOrder`、`SoapMedication` 公開模型。未辨識段落、明確空白、未知資料列分開呈現；診斷碼不推論 ICD 版本或主次，醫囑摘要不取代可開啟報告的完整醫囑操作。
- 離線重解析使用相同 Parser，新增只含欄位名稱、筆數及問題碼的結構摘要。合成測試與既有 HAR 離線驗證已補齊，後續取得兩份內網測試回傳。
- 新增 `soap` 測試計畫：依登入卡號取得 2026-09-21 專屬門診，抽多名不同病人逐一確認實際就診並取得結構化 SOAP；原清單、各病人就診清單、SOAP 及欄位覆蓋率分階段保留。單筆失敗續跑，無需手動提供病歷號，不執行錯誤密碼或異動。
- Windows x64 `soap` 預設計畫 EXE 曾以四個合成病歷號在 HTTPS localhost 的完整與部分失敗情境通過；第一份院內 ZIP 後續揭露前置說明藥囑表格式。
- 2026-09-23 內網回傳證實四筆 SOAP 的 S／O／A+P 與診斷可解析，但其中一筆藥囑表前有連續處方說明，舊解析器漏列藥囑且未報問題。修正為從明確欄位標題辨識表格並保留前置說明；原 ZIP 保持不變。另兩筆就診清單混入異病歷號連結，仍維持跨病人安全阻擋。
- 連續處方說明中的「服藥期限」另以 `SoapChronicPrescriptionPeriod` 回傳起訖日及來源區塊索引；一般處方維持空清單，格式不合法時回解析問題碼。第二份內網回傳確認修正後 EXE 對四份 SOAP 的 S／O／A+P、診斷與藥囑解析成功，其中一份有慢性處方日期區間；兩份就診清單的異病歷號連結仍由安全檢查阻擋。

## 0.19.4

- 登入測試器可將人事顯示名稱對應至當次表單的「選項代碼 - 名稱」，只剝除與該選項 value 相同的前綴；重複名稱仍保留缺口，避免錯選單位。無對應時分別記錄單位與下層單位未測。
- 離線缺口報告保留已知的人事測試步驟名稱與原因碼，避免只看到 personnel.search 而無法辨識是哪個條件未測。原始 ZIP 狀態不改寫。
- 文件改以目前功能及驗證範圍為主，整合建置／發布說明，移除過時的雙擊測試範圍與重複歷史敘述。0.19.3 內網已驗證登入拒絕、PRQ Session 恢復及主要人事查詢；單位及下層單位仍待內網驗證。

## 0.19.3

- 依 0.19.2 內網回傳修正三處辨識：人事 SSO 同時接受 `frame`／`iframe`；錯誤密碼的文字錯誤頁回傳 `LoginRejectedError`；Cookie 過期的入口轉址觸發既有的一次重新登入。
- 主系統只解析 script 中頂層的字串指定，忽略 onclick、註解、字串範例與未執行的 callback，不再把「重新登入」按鈕導向 logout 當成登入成功；登入目的頁須是已錄製的 myPortal.do。
- JSON 查詢預設攔截轉址，辨識已知 Portal／舊入口後從原 HTTPS 登入流程恢復，避免跟隨到 HTTP 入口。已自行處理轉址的 Adapter 保留原流程；未知轉址保留獨立錯誤，不猜測帳密或反覆登入。
- 離線分析保留轉址鏈對應的原查詢與過期原因，不改寫原始 EXE 狀態。登入模擬增為 20 種，本機 EXE 情境涵蓋文字拒絕頁、iframe 及實際 302 過期形狀；後續 0.19.3 內網回傳確認恢復、人事查詢及兩次預期登入拒絕成功，單位條件的測試輸入對照仍有缺口。

## 0.19.2

- 新增 `login` 專項 EXE 計畫：正常登入、九個登入目標檢查（含主系統）、登入重用、清除本機 Cookie 後的查詢恢復，以及本人人事卡號／姓名／職稱／單位查詢。
- 最後最多做兩次真實錯誤密碼測試，分別走明確登入與按需查詢登入。每次使用獨立 Session，攔截實際密碼 POST 的重送；正常登入失敗，或第一次負向結果不明時，不再追加刻意失敗。
- 內建 17 個無網路模擬情境，涵蓋輸入、拒絕、HTTP、轉址、逾時及有限次 Session 恢復。各步驟續跑、保存結果；未真正觸發 Session 過期記 NO_SAMPLE，不宣稱自然逾時已驗證。
- 離線分析按已完成的負向步驟及 capture 範圍辨識預期拒絕，保留原始錯誤證據；其他登入／查詢錯誤照常列出。ZIP 維持未加密、含時間、直接放在 EXE 同目錄，無額外設定檔或 SHA256。

## 0.19.1

- 區分 `LoginRejectedError` 與 `AuthExpiredError`：主系統登入遭拒不再觸發第二次密碼提交；真正的查詢 Session 過期仍保留一次重登入與 SSO／context 重建。
- 重登入時保留具體的登入失敗碼，不以籠統的 `AUTH_RELOGIN_FAILED` 覆蓋。登入流程的 HTTP 401／403、空回應及系統錯誤不會被當成既有 Session 過期或直接宣告帳密錯誤。
- 主系統登入轉址改為同來源、有限次的 GET 導覽，阻止 307／308 重送密碼及未知目的頁被視為登入成功。審查 OAuth 送出密碼後的 HTTP 拒絕也不會重新開始整段登入。
- MIS 與審查登入遭拒共用公開例外型別；PortalCredentials 的 repr 不再顯示密碼。補充錯誤處理用法、離線重解析與合成回歸；本版尚無新的內網回傳證據。

## 0.19.0

- 新增 `sdk.personnel`：動態職稱／單位選項、多條件人事查詢、精確員工卡號查找；分開保留員工編號與醫師章號，取得姓名後可組合就診篩選。
- 依新 HAR 接上 DDPortal SSO 與 Big5 查詢，沿用共用 TLS、Session、節流及登入恢復。新增兩個唯讀目錄項目、測試器輸入發現、離線重解析與合成測試。
- 就診醫師篩選文件以姓名為準；保留舊 raw vsNo 參數相容，增量測試不再要求該可選欄位。人事功能已通過合成與 HAR 離線驗證，尚待新版 SDK 內網實測。

## 0.18.1

- 就診清單先選擇網頁中可靜態判定的實際 JavaScript 分支，再解析與去重；未啟用分支不會經由舊版連結 fallback 重新混入。不執行 JavaScript，也不合併不同分支的醫師資訊。
- 修正 0.18.0 回傳被誤列為醫師卡號 NO_SAMPLE 的情況：同一份原始資料可離線取回住院／急診的卡號及正確導覽參數；門診缺卡號仍保留空白。兩種病人輸入的清單與醫師姓名維持一致。
- 0.18.0 內網回傳確認相容 TLS 優先策略已消除原有六次失敗探測；本輪所有連線保留 HTTPS 與憑證驗證。新分支選擇修正另列為離線／本機驗證，不改寫原 EXE 的缺口狀態。

## 0.18.0

- SDK 共用傳輸層自動選擇 TLS：PRQ／SectOrd／WebMAAS 第一個請求使用已驗證的 TLS12_COMPAT，同源共用且保留 Session／Cookie；一般使用不需手動 configure_connection。
- 唯讀請求在有限次數內切換協定，明確憑證錯誤可對已設定的來源使用略過驗證的 HTTPS。預設 allow_unverified_tls=True，嚴格模式 False 可關閉該備援；不改 HTTP，不全域關閉驗證。
- 第一次不可重試 POST 前匿名確認連線；登入與異動本身不因 TLS 自動補送。新增 connection_status，記錄每個服務實際選擇。
- 測試 EXE 預檢成功即停止，同源重用結果；raw capture 保留匿名探測及已恢復失敗，離線分析不將已恢復網路失敗當成未解錯誤。
- 離線分析將 NO_SAMPLE 獨立列為驗證缺口，只有缺口時保留 COMPLETED_WITH_GAPS 並回傳 exit code 0；不再將缺欄位誤報為執行錯誤，真正的回應錯誤仍照常報告。

## 0.17.2

- 修正病人標頭將網址分段串接時，`&hidno=...` 未被辨識成 query 而誤報 PRQ_PATIENT_ID_MISMATCH。保留完整身分比對，缺失、空白或衝突的識別值仍拒絕。
- 0.17.2 內網回傳確認身分證分支修正：病歷號／身分證兩份清單一致，四欄篩選及從身分證清單選定門診取得 SOAP／醫囑成功；醫師卡號因來源未提供而保留 NO_SAMPLE。
- 合成病人標頭改用實際遇到的 URL 串接結構，供 Parser、Adapter、離線重解析及 localhost EXE 共用回歸。

## 0.17.1

- 新增 visits 增量測試：比較病歷號／身分證就診清單，保留四欄篩選及最多三筆門診的 SOAP／醫囑串接證據。
- 可從基本資料自動取得病人身分證或啟動時手動輸入；錯誤、無樣本及替代路徑分開記錄，各項失敗不阻止獨立檢查。
- EXE 可用 `--default-profile visits` 建置成雙擊即測新增功能的版本；沿用無設定檔搬移、未加密且含時間的同目錄 ZIP。
- 離線重測建議保留 visits 計畫，避免退回只查病歷號而漏測身分證分支。

## 0.17.0

- `records.get_visit_cases`／`find_visit_cases` 支援 `national_id`，與病歷號二擇一；依錄製的前端 type=2 表單查詢，核對病人標頭身分並解析實際病歷號。此新增連線分支尚待內網實測。
- VisitCase 增加 doctor_name、doctor_card、case_type_label；完整解析 KSCase 的醫師欄與串接 URL，保留就診參照。
- VisitFilter 支援到院日期、O／A／E 類別、科別、醫師姓名／卡號的組合篩選；可重複篩選同份清單而不增加 requests。
- 病人不符明確報錯；身分證查詢逾時重登入會重建及核對病人 context。離線 HAR／ZIP 重解析支援病人標頭。
- SOAP 明確限制已錄製的門診路徑；列出住院／急診不代表已有其 SOAP／醫囑串接。補充使用範例、原子 API 與驗證範圍。

## 0.16.0

- 整理可公開的 SDK、MIT 授權、CI、原子 API 參考、組合開發及 HAR 擴充文件。
- 移除真實測試識別值；LiveTestConfig.test_mrn 可由 CLI／JSON／環境設定，自用 EXE 可內嵌私有預設。
- 修正 MIS 巢狀資料表被忽略，保留主表的數值欄位。
- 修正離線分析對空門診入口及 MIS USR_ID SSO 欄位的誤判。
- 沒有可用參照的測試標 NO_SAMPLE，整體可標 COMPLETED_WITH_GAPS；已恢復的 TLS 探測不當成未解錯誤。
- comprehensive 的原子門診日期涵蓋最近七天；完整 SOAP 組合流程仍由明確設定控制。
- HAR contract 從請求取得病人 context，移除固定病人依賴。

## 0.15.x 以前

本機開發階段建立 Requests 登入與 SSO、病人／掛號／就診、醫囑與報告、手術案例／PDF、審查系統及 MIS 報表。0.16.0 為整理後的首次公開原始碼版本；歷史私有 HAR 與內網回傳不納入 Git。
